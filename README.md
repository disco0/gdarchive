# gdarchive

GDNative library for reading and writing zip archives in Godot `3.x`. Includes `4.0` `ZIPReader`/`ZIPPacker` classes as well as an additional `ZIPWriter` class for full folder content archives.